#!/bin/bash

# TODO: this should parse the logs for 'is a paper'

BIN="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source $BIN/common-defs.sh

declare infile="$1"
declare outfile="$2"

# find $infile -type f -name "*.pstotext.log" -exec grep 'is a paper' {} ';' -printf "%p -> %p.meta.xml\n" > $workdir/$outfile

find $infile -type f -name "*.pstotext.log" -exec grep 'is a paper' {} ';' -printf "%p -> %p.meta.xml\n"


