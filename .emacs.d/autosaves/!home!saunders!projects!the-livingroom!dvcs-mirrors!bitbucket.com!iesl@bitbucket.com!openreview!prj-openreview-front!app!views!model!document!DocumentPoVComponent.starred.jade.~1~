- import controllers.DocumentAction
- import org.joda.time.format.DateTimeFormat
- import model.GlobalFormats.dateformat
-@ val it : model.document.DocumentPoVComponent
- import it._


-# - if(worldReadable) {
-# a.ajax(href={"/document/" + uuid + "/sharing/index"} rel="tooltip" title="World readable" margin="10px")
-#   i.icon-globe
-# - } else {
-# a.ajax(href={"/document/" + uuid + "/sharing/index"} rel="tooltip" title={visibleTo} margin="10px")
-#   i.icon-lock
-# - }

- if(worldReadable) {
i.icon-globe.withtooltip(title="World readable")
- } else {
i.icon-lock.withtooltip(title={visibleTo})
- }
