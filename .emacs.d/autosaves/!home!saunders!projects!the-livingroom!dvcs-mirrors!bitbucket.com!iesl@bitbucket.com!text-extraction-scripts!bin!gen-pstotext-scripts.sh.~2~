#!/bin/bash

BIN="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

source $BIN/common-defs.sh

cd $workdir

find . -type f -name "run-pstotext.*" -exec $bin/make-pstotext-script.sh {} ';'

# for f in run-pstotext.*

