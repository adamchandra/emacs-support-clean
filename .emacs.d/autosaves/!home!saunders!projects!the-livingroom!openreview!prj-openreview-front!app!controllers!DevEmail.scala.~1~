package controllers

import lib._
import lib.core._

import play.api._
import play.api.mvc._


object DevEmail extends Controller with ControllerOps with ScalateOps {

  def index() = OptUserAction { implicit request => implicit user =>
    Ok(scalate("dev/email")(
      'request -> request,
      'user -> user
    ))
  }

}

