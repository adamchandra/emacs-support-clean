.btn-group
  button.btn.btn-small(data-toggle="dropdown" href="#")
    - it.primaryEmail.map(email=>
      | #{email.email}
    - ).getOrElse(
      | No verified email
    - )

  button.btn.btn-small.btn-primary.dropdown-toggle(data-toggle="dropdown") 
    span.caret

  ul.dropdown-menu
    - for (email <- it.nonPrimaryEmails)
      - val encEmail = URLEncoder.encode(email.email, "UTF-8")
      li
        a.ajax(href="/user/email/setprimary/#{encEmail}")
          | #{email.email}
