.row-fluid
  .span12
    a.btn.ajax(id={uuid + action.id}
               href={"/admin/sendReviewerInvites"}
               rel="tooltip"
               title={action})
      i(class={action.icon})
      | Send Reviewer Invites
