package org.scalajars.web.controllers

import org.scalajars.web.lib._

import play.api._
import play.api.mvc._

object LibraryController extends Controller with ControllerOps with ScalateOps {
  def index() = OptUserAction { implicit request => implicit user =>
    Ok(
      scalate("library")('user -> user)
    )
  }
}

