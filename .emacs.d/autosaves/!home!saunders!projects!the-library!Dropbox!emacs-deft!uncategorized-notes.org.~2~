* uncategorized notes


*** Perpetual TODO: Get items out of this file and properly categorized

* mongo -> solr script

mongoexport -h localhost -d rexa2-dev-acs -c paperMentions --jsonArray -q "{"year" : 2006}" | curl 'http://localhost:8080/solr/update/json?commit=true' --data-binary - -H 'Content-type:application/json'
java -Dsolr.solr.home=/home/saunders/projects/the-livingroom/rexa2s/solr -Dsolr.solr.install.dir=/home/saunders/projects/the-livingroom/rexa2s/apache-solr/apache-solr-3.5.0 -jar start.jar


*** Nodejs install
    github version in outhouse

    - currently using nodejs:
      TermKit
    
      
*** Visualization system for scala code execution, e.g., step-wise
    function execution, composed function visualization, etc.
    
    function def:
    A => B
    #{A.type} => #{B.type}
    
    RenderT((_:String).reverse)


*** [[http://dspace.mit.edu/bitstream/handle/1721.1/41867/Monod-Soergel-Oct2004.pdf?sequence=1][Soergal paper on collaboratively working w/Bio Knowledge]]

*** Make treezip/unzip write info to root of zipped tree, about src/target dirs, 
    to make it more obvious where files came from, e.g., 
    [File contents]
    zipped:id1:../../proj2/src/.../f1
    zipped:id1:../../proj2/src/.../f2
    zipped:id2:../../proj9/src/.../f32


*** Ensime/sbt plugin ideas
    - [ ] Write an sbt/ensime plugin that generates lenses from case classes, 
            and just spits out the code for inclusion in the source file.
    - [ ] Automatically write out and remove type annotations on variables


*** Start creating slides for presenting various topics on programming, scala, 
    parallelism, etc, using the impress.js (or other) infinite-plane slideshow concept
    -- note: impress.js is very unimpressive...

*** cc.acs.commons.parsers:
    + canned parsec and regex parsers for 
      name, email, author lists, compound emails, etc.

*** Regex + trigam indexing makes for very fast regex search:
    based on google lab's codesearch engine, now defunct.
    http://swtch.com/~rsc/regexp/regexp4.html

*** Serialization for factorie
    SJson serialization? 
    https://github.com/debasishg/sjson


All my gists:

curl -i -u "adamchandra:password" https://api.github.com/users/adamchandra/gists

Get a particular gist as json:

curl -i -u "adamchandra:pass" https://api.github.com/gists/2138295


